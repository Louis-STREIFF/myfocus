{% extends 'base.html.twig' %}

{% block title %}Modification des préférences{% endblock %}

{% block body %}
{{ form_errors(preferenceForm) }}

    {{ form_start(preferenceForm) }}
        {{ form_row(preferenceForm.firstname) }}


        <button type="submit" class="btn">Valider</button>
    {{ form_end(preferenceForm) }}
    <div class="container">
        <h1> Modification des préférences </h1>

        <h3> La ville </h3>
        <p> {{$user.city}}</p> 

        <h3> Les centres d'intérêt </h3>
        <p> {{$user.favoriteKeywords}}</p>
        
        <h3> vos objectifs du jour </h3>           
        <ul class="list-group">
            {{ include('objective/_form.html.twig') }} 
        </ul>
    </div>
{% endblock %}